@using Henagone_WebGame.Models.GameViewModels
@{ 
    List<NarrativeQuest> ItemsList;
    using (var context = new SiteDbContext())
    {
        ItemsList = context.NarrativeQuest
            .OrderByDescending(r => r.Id)
            .Select(r => r).ToList();
    }
}

<div class="col-md-6">
    <div class="panel panel-default panel-border-color panel-border-color-primary">
        <div class="panel-heading panel-heading-divider">Liste des codes cadeaux</div>
        <div class="panel-body table-responsive">
            <table class="table table-striped table-borderless">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Titre</th>
                        <th>Déroulement</th>
                        <th class="actions"></th>
                    </tr>
                </thead>
                <tbody class="no-border-x">
                    @foreach (var item in ItemsList)
                    {
                        <tr id="@("tableItemTr" + item.Id)">
                            <td>@item.Id</td>
                            <td>@item.TitleFr </td>
                            <td>@item.StepList</td>
                            <td class="actions">
                                <a href="~/Admin/NarrativeQuest/@item.Id" data-placement="top" data-toggle="tooltip" title="Edition"><span class="icon mdi mdi-edit"></span></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>